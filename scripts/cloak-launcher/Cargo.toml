[package]
name = "cloak-launcher"
version = "0.1.0"
edition = "2021"
description = "BeehiveBrowser 启动器 — 启动伪装浏览器窗口"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "cloak-launcher"
path = "src/main.rs"
